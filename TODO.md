Health check endpoint (/health)
Graceful shutdown with OS signals (SIGTERM/SIGINT)
Validate critical config values exist at startup
Magic numbers as configurable constants (15min JWT, 7d refresh)
Rate limiting on auth endpoints
Structured logging (JSON for production)
More tests (only repo tests exist, missing service and handler tests)
Request ID / correlation ID for traceability
Makefile with common commands
